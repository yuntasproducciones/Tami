---
import type Producto from "src/models/Product";
/* 
const response = await fetch("https://tu-dominio.com/api/products");
const products: Producto[] = await response.json();
const firstThree = products.slice(0, 3); // Tomar solo los 3 primeros */

const arrayDePrueba: Producto[] = [
  {
    id: "1",
    nombreProducto: "Wazaa",
    stockProducto: 0,
    precioProducto: 0,
    image: "",
  },
  {
    id: "2",
    nombreProducto: "Wazaa2",
    stockProducto: 0,
    precioProducto: 0,
    image: "",
  },
  {
    id: "3",
    nombreProducto: "Waza3",
    stockProducto: 0,
    precioProducto: 0,
    image: "",
  },
];
---

<ul class="w-full h-full flex justify-between gap-10">
  {
    arrayDePrueba.map((product) => (
      <li class="bg-white rounded-t-3xl w-full h-full overflow-hidden text-center font-bold text-4xl">
        <a href={`/products/${product.id}`} class="w-full h-full text-teal-700">
          <img src={product.image} alt={product.nombreProducto} class="h-4/6" />
          {product.nombreProducto}
          <button class="border-teal-600 border text-base rounded-full w-fit place-self-center px-4 py-1 block hover:border-0 hover:bg-teal-700 hover:text-white">
            INFORMACIÓN
          </button>
        </a>
      </li>
    ))
  }
</ul>
